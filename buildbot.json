{
    "build": {
        "sdist": [
            "pip install -r requirements.txt",
            "python setup.py sdist"
        ],
        "install": [
            "pip install -v source.tar.gz"
        ],
        "wheel": [
            "python untar.py source.tar.gz src",
            "python src/setup.py bdist_wheel",
            "python mvdist.py src/dist",
            "pip install -v *.whl"
        ]
    },
    "test": {
        "after": ["install", "wheel"],
        "package": "preshed",
        "args": "--tb=native"
    }
}
