language: python
python:
  - "2.7"
  - "3.4"
  - "pypy"
# command to install dependencies
#
install:
  - "pip install --upgrade setuptools"
  - "pip install -e ."
# command to run tests
script: "py.test preshed"
